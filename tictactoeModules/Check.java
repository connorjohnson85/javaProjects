package tictactoeModules

class Check {
  public static boolean checkWin(string playerName, String[] board) {
    // Checks the board to see if we win, returns a true if a player has winned, and returns a false if no one wins. 

  }
}
